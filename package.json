{
  "name": "price-engine",
  "version": "1.0.0",
  "description": "Standalone API that scrapes secondhand market prices from Vinted, eBay, and Vestiaire Collective.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
